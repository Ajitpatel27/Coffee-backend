# MongoDB connection string used by the API routes. Never commit a real password.
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.2tyyycf.mongodb.net/coffee-website?appName=Cluster0

# Local development only; Vercel supplies its own port
PORT=5001
